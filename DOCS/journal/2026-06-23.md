<!-- PRESERVATION RULE: Never delete or replace content. Append or annotate only. -->

## 2026-06-23 — Unreleased batch

### Content ingest architecture (site-wide)

- **New:** `DOCS/INGEST_WORKFLOW.md` — agent runbook (research → intake JSON → verify → import).
- **New:** `DOCS/schemas/intake-record.schema.json`, `DOCS/intake/_TEMPLATE.intake.json`, `DOCS/intake/_EXAMPLE.intake.json`.
- **New:** `tools/verify_intake.py` — deterministic pre-import gate (stdlib only; `python tools/verify_intake.py`).
- **Wired:** `AGENTS.md` §4/§10, `DOCS/ARCHITECTURE.md` § Content ingest pipeline, `DOCS/CONTENT_GUIDE.md`, `.cursor/rules/otterdays-agents.mdc`, `DOCS/SUMMARY.md`, `DOCS/CHANGELOG.md` `[Unreleased]`.
- **Policy:** No HTML / `search-data.js` edits until intake verify **PASS** (unless user skips intake).

### Site content (same day)

- **North** (Cohere) — `other-assistants.html` (Canada), `companies.html`.
- **Subquadratic** — `companies.html` (SubQ API, Code, Search); **SubQ Code** on `tools.html` CLI.
- **Common tools** — Homepage Git / Python / Bun; `tools.html#common-tools`.
